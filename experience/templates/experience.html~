{%extends "base.html"%}
{%load static%}
{%block page_content%}
<
<h1>Experience</h1>
<div class="row">

{%for experience in experiences%}
    <div class="col-md-4">
        <div class="card mb-2">
            <img class="card-img-top" src="{% static experience.img %}">
            <div class="card-body">
              <h5 class="card-title">{{ experience.title }}</h5>
	      <h6 class="card-text">{{ experience.company }}</h6>
              <p class="card-text">{{ experience.desc }}</p>
              <a href="#"class="btn btn-primary">Read More</a>
            </div>
        </div>
    </div>
    {%endfor %}
</div>
{%endblock %}
